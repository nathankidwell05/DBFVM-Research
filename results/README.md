# Versioned results

Only compact result tables are stored here. Large `.mat` files and generated
figures are excluded because the committed MATLAB drivers reproduce them.

`d1v5_gminmod_L2_convergence_results.csv` records the completed four-grid
generalized-minmod study at `theta = 1.20`, `tau = 5e-5`, and `CFL = 0.10`.
